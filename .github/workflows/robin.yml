on:
  pull_request:
    types: [opened, synchronize]

jobs:
  run-robin:
    uses: my-org/shared-actions/.github/workflows/robin-review.yml@main
    secrets:
      LLM_API_KEY: ${{ secrets.LLM_API_KEY }}