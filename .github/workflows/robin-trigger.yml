name: Robin Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  run-robin:
    uses: robin-review-shared/shared-actions/.github/workflows/robin-shared.yml@main
    secrets:
      LLM_API_KEY: ${{ secrets.LLM_API_KEY }}